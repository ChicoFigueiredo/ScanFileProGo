{
  "name": "scanfile-ui",
  "private": true,
  "version": "0.0.0",
  "description": "Interface web do ScanFile Pro. Sem dependencias: os testes usam node:test.",
  "type": "commonjs",
  "scripts": {
    "test": "node --test \"tests/*.test.mjs\""
  }
}
